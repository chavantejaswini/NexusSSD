import { PagePlaceholder } from "../components/PagePlaceholder";

export function FleetOverview() {
  return (
    <PagePlaceholder
      title="Fleet Overview"
      description="Health KPIs, at-risk drives, and telemetry trends across the SSD fleet."
      phase="Phase 6"
    />
  );
}
