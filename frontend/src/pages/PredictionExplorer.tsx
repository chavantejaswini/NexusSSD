import { PagePlaceholder } from "../components/PagePlaceholder";

export function PredictionExplorer() {
  return (
    <PagePlaceholder
      title="Prediction Explorer"
      description="Sortable, filterable table of drive failure probabilities with contributing SMART features."
      phase="Phase 6"
    />
  );
}
