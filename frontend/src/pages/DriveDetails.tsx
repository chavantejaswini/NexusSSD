import { PagePlaceholder } from "../components/PagePlaceholder";

export function DriveDetails() {
  return (
    <PagePlaceholder
      title="Drive Details"
      description="Per-drive SMART telemetry charts, latest failure prediction, and maintenance history."
      phase="Phase 6"
    />
  );
}
